name: Reusable – Docker Linux

on:
  workflow_call:
    inputs:
      release_version:
        required: true
        type: string
      registry:
        required: true
        type: string
      image_name:
        required: true
        type: string

defaults:
  run:
    shell: bash

jobs:
  docker-linux:
    runs-on: ubuntu-latest
    permissions:
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ inputs.registry }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Build and push linux/amd64
        env:
          RELEASE_VERSION: ${{ inputs.release_version }}
          REGISTRY: ${{ inputs.registry }}
          IMAGE_NAME: ${{ inputs.image_name }}
        run: |
          IMAGE=${REGISTRY}/${IMAGE_NAME}
          docker build \
            --platform linux/amd64 \
            --file .docker/Dockerfile \
            --build-arg APP_VERSION="${RELEASE_VERSION}" \
            --build-arg REVISION="${{ github.sha }}" \
            --tag ${IMAGE}:${RELEASE_VERSION}-linux-amd64 \
            .
          docker push ${IMAGE}:${RELEASE_VERSION}-linux-amd64
          mkdir -p /tmp/digests
          docker inspect --format='{{index .RepoDigests 0}}' ${IMAGE}:${RELEASE_VERSION}-linux-amd64 | awk -F@ '{print $2}' > /tmp/digests/linux-amd64

      - name: Build and push linux/arm64
        env:
          RELEASE_VERSION: ${{ inputs.release_version }}
          REGISTRY: ${{ inputs.registry }}
          IMAGE_NAME: ${{ inputs.image_name }}
        run: |
          IMAGE=${REGISTRY}/${IMAGE_NAME}
          docker build \
            --platform linux/arm64 \
            --file .docker/Dockerfile \
            --build-arg APP_VERSION="${RELEASE_VERSION}" \
            --build-arg REVISION="${{ github.sha }}" \
            --tag ${IMAGE}:${RELEASE_VERSION}-linux-arm64 \
            .
          docker push ${IMAGE}:${RELEASE_VERSION}-linux-arm64
          docker inspect --format='{{index .RepoDigests 0}}' ${IMAGE}:${RELEASE_VERSION}-linux-arm64 | awk -F@ '{print $2}' >> /tmp/digests/linux-arm64

      - name: Upload linux digests
        uses: actions/upload-artifact@v4
        with:
          name: digests-linux
          path: /tmp/digests/*
          if-no-files-found: error
